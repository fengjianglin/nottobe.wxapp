<view class="container">
  <block wx:for="{{follows}}" wx:key="follows" wx:for-item="item" wx:for-index="itemIdx"> 
    <user user="{{item.follower}}"></user>
  </block>
</view>
<view class="bottom" wx:if="{{isLastPage}}">底线到了...</view>